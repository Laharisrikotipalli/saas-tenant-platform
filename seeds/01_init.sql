CREATE EXTENSION IF NOT EXISTS "pgcrypto";

CREATE TABLE IF NOT EXISTS tenants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL UNIQUE,
  created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) NOT NULL UNIQUE,
  tenant_id UUID NOT NULL REFERENCES tenants(id),
  created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS user_roles (
  user_id UUID PRIMARY KEY REFERENCES users(id),
  role VARCHAR(50) NOT NULL CHECK (role IN ('admin', 'member'))
);

CREATE TABLE IF NOT EXISTS projects (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL,
  tenant_id UUID NOT NULL REFERENCES tenants(id)
);

CREATE TABLE IF NOT EXISTS project_users (
  project_id UUID NOT NULL REFERENCES projects(id),
  user_id UUID NOT NULL REFERENCES users(id),
  PRIMARY KEY (project_id, user_id),
  UNIQUE (project_id, user_id)
);

INSERT INTO tenants (id, name) VALUES
  ('a0000000-0000-0000-0000-000000000001', 'TenantA'),
  ('b0000000-0000-0000-0000-000000000002', 'TenantB'),
  ('c0000000-0000-0000-0000-000000000003', 'DefaultTenant')
ON CONFLICT DO NOTHING;

INSERT INTO users (id, email, tenant_id) VALUES
  ('a1000000-0000-0000-0000-000000000001', 'admin@tenanta.com', 'a0000000-0000-0000-0000-000000000001'),
  ('a2000000-0000-0000-0000-000000000002', 'member@tenanta.com', 'a0000000-0000-0000-0000-000000000001'),
  ('b1000000-0000-0000-0000-000000000001', 'user@tenantb.com', 'b0000000-0000-0000-0000-000000000002'),
  ('c1000000-0000-0000-0000-000000000001', 'test@default.com', 'c0000000-0000-0000-0000-000000000003')
ON CONFLICT DO NOTHING;

INSERT INTO user_roles (user_id, role) VALUES
  ('a1000000-0000-0000-0000-000000000001', 'admin'),
  ('a2000000-0000-0000-0000-000000000002', 'member'),
  ('b1000000-0000-0000-0000-000000000001', 'member'),
  ('c1000000-0000-0000-0000-000000000001', 'admin')
ON CONFLICT DO NOTHING;

INSERT INTO projects (id, name, tenant_id) VALUES
  ('a0000000-0000-0000-0000-000000000011', 'Project Alpha', 'a0000000-0000-0000-0000-000000000001'),
  ('b0000000-0000-0000-0000-000000000022', 'Project Beta', 'b0000000-0000-0000-0000-000000000002')
ON CONFLICT DO NOTHING;
